我计划开发一个开源软件，点对点的即时通讯软件GungChat（敢说）。不用注册；加密通讯；阅后即焚；点对点；文字、图片、语音、视频通话；通过手机或者IP地址连接，无服务器；知道对方的手机或者IP地址，就请求连接通话。注意语音和视频通话的优化压缩技术，视频不大于320p。 无群聊功能。主打点对点的完全通讯。局域网优先，然后手机数据计划。
先请帮我产生一个计划书。然后是Software Specification Document.


下面是为 GungChat（敢说） 量身定制的项目计划书与软件规格说明（SRS）草案。我把内容分成六个清晰部分：项目概述、路线与里程碑、软件规格（功能与非功能需求）、架构与协议、安保与隐私、测试与部署维护。每一节都尽量做到可执行、可交付，便于直接转成项目文档、任务卡或开发里程碑。

项目概述
项目名称：GungChat（敢说）
定位：开源、点对点（P2P）、无需注册的即时通讯工具。
核心卖点：无需注册；端到端加密；阅后即焚；纯点对点（无服务器）；支持文字、图片、语音、视频通话；局域网优先，移动数据次之；无群聊功能。
目标用户：注重隐私与点对点通信的个人用户、技术爱好者、对等通信场景（局域网内文件/语音/视频直连）。
关键约束：视频分辨率不超过 320p；语音/视频需优化压缩与带宽适配；不提供群聊；连接基于对方手机号或 IP 地址；优先局域网直连，穿透 NAT 时使用 P2P 穿透技术（STUN/ICE/UDP hole punching）；无常驻服务器（仅可选性使用信令中继或穿透辅助）。

路线与里程碑（建议 4–6 个月 MVP 路线）
阶段 0 — 需求与原型（2 周）

完成详细需求文档（SRS 初稿）与 UI/UX 低保真原型。

确定支持平台（Android、iOS、桌面 Linux/Windows/macOS）。

阶段 1 — 核心 P2P 通信与加密（4 周）

实现局域网发现（mDNS/UDP broadcast）与基于 IP/端口的直连。

集成端到端加密（基于 X25519 + AES-GCM 或 libsodium）。

简单文本消息收发与阅后即焚机制。

阶段 2 — 媒体支持与通话（6 周）

图片传输（分片、断点续传、加密）。

语音通话（Opus 编解码器，低延迟模式，回声抑制）。

视频通话（VP8/VP9 或 H.264 低码率配置，限制 320p，分辨率/帧率自适应）。

NAT 穿透（STUN/ICE 实现），可选 TURN 中继（仅在用户允许时）。

阶段 3 — 稳定性、优化与跨平台（6 周）

优化压缩、带宽自适应、抖动缓冲、重传策略。

电池与移动数据优化（后台策略、网络切换处理）。

桌面与移动客户端 UI/UX 打磨。

阶段 4 — 测试、文档与开源发布（4 周）

完整测试（单元、集成、互操作、穿透场景）。

发布开源仓库、开发者文档、用户手册。

交付物：MVP 客户端（移动 + 桌面）、开源仓库、SRS 完整版、测试报告、示例部署/穿透指南。

软件规格说明（SRS）—— 功能与非功能需求
1. 范围与目标
目标：实现一个点对点即时通讯客户端，强调隐私与直接连接，支持文本、图片、语音、视频通话，且不依赖常驻服务器。

2. 功能需求（高优先级）
F1 局域网发现：通过 mDNS/UDP 广播发现局域网内设备并发起连接请求。

F2 直接 IP 连接：支持通过已知手机号（映射到 IP/端口）或直接 IP 地址发起连接。

F3 端到端加密：所有消息与媒体均使用端到端加密（密钥协商基于 X25519/ECDH，消息加密用 AES-GCM 或 ChaCha20-Poly1305）。

F4 阅后即焚：消息可设置阅后即焚（阅读后在双方设备删除并销毁密钥材料）。

F5 文本消息：可靠、有序传输，支持回执（已送达/已读）。

F6 图片传输：分片、加密、断点续传、缩略图预览（缩略图同样加密）。

F7 语音通话：Opus 编解码器，回声抑制、自动增益控制（AGC）、噪声抑制。

F8 视频通话：分辨率上限 320p，支持自适应码率与帧率，优先使用硬件编码（若可用）。

F9 NAT 穿透：实现 STUN/ICE，支持 UDP hole punching；可选 TURN 中继（仅在用户明确允许并自托管时）。

F10 无群聊：系统不提供群聊或多点会议功能（仅点对点）。

F11 无注册：无需账号注册，使用临时密钥对或设备指纹进行身份验证。

F12 日志与隐私：默认不保留消息日志；若启用日志，必须本地加密并可一键清除。

3. 非功能需求
N1 延迟：语音通话端到端延迟 < 200ms（局域网内目标 < 50ms）。

N2 带宽：语音目标 16–64 kbps；视频 150–600 kbps（320p 自适应）。

N3 可靠性：消息传输成功率 > 99%（在正常网络条件下）。

N4 安全：抗中间人（MITM）攻击、前向保密（PFS）、密钥材料不持久化到云端。

N5 兼容性：支持主流 Android/iOS 及桌面平台。

N6 资源占用：移动端 CPU 与电量消耗需优化，后台运行时尽量降低资源占用。

N7 可维护性：模块化代码、清晰接口、自动化测试覆盖关键路径。

4. 用户故事（示例）
US1：A 知道 B 的手机号，A 发起点对点连接并进行加密语音通话，通话在局域网内直连。

US2：A 向 B 发送一张图片并设置阅后即焚，B 查看后图片在双方设备删除。

US3：A 与 B 在 NAT 后面，使用 STUN/ICE 成功穿透并建立 P2P 视频通话（320p）。

架构与协议（技术选型建议）
总体架构：纯 P2P 客户端架构，模块化分层：UI 层 → 应用逻辑 → 媒体处理（编解码）→ P2P 连接管理 → 加密层 → 网络传输层。

协议与库建议：

加密：libsodium（X25519、ChaCha20-Poly1305）或 libsodium + AES-GCM（视平台硬件加速）。

信令/发现：mDNS/UDP 广播（局域网）；基于 WebRTC 的 STUN/ICE 实现用于穿透（可借鉴 libwebrtc 的 ICE 实现）。

媒体编解码：Opus（语音），VP8/VP9 或 H.264（视频，320p 限制）；使用 libwebrtc 或 FFmpeg（桌面）/MediaCodec（Android）/VideoToolbox（iOS）做硬件加速。

传输层：优先 UDP（实时媒体），必要时使用 QUIC 或 TCP 做可靠传输（文本、图片分片）。

文件传输：分片 + 校验（SHA-256） + 重传机制。

NAT/穿透：STUN/ICE；可选自托管 TURN（仅在用户允许并自托管时）。

数据模型（本地）：

设备标识：临时公钥指纹 + 可选用户别名（本地保存）。

会话：会话 ID（本地生成）、对端公钥、会话密钥、过期时间、阅后即焚标志。

消息：消息 ID、类型（文本/图片/语音/视频）、加密负载、元数据（时间戳、过期时间）。

安全与隐私
核心原则：最小化数据持久化、端到端加密、前向保密、用户可控的日志与密钥销毁。

关键措施：

密钥协商：基于 X25519/ECDH，支持 PFS（会话密钥短期有效）。

消息加密：每条消息使用独立对称密钥（AES-GCM/ChaCha20-Poly1305），密钥由会话密钥派生。

阅后即焚：消息阅读后触发本地删除与密钥销毁；若对端未响应，设置超时自动销毁。

最小化元数据：尽量不记录通信元数据；若需记录（调试），必须本地加密并可清除。

可选 TURN：若用户启用 TURN 中继，明确告知会产生中继流量并需自托管或信任第三方。

合规与开源：开源许可建议采用 MIT 或 Apache 2.0（便于社区贡献与商用），并在 README 中明确隐私与安全声明。

测试、部署与维护
测试策略：单元测试、集成测试、互操作测试（不同网络环境）、穿透场景测试（NAT 类型矩阵）、性能测试（延迟、带宽、CPU、内存、电量）、安全审计（第三方代码审计）。

部署建议：

开源仓库结构：/client-mobile-android、/client-mobile-ios、/client-desktop、/libs/crypto、/docs、/ci。

CI/CD：自动化构建与测试（GitHub Actions / GitLab CI），发布二进制与安装包。

文档：用户手册、开发者指南、协议说明、穿透与自托管 TURN 指南。

维护与社区：建立贡献指南、代码风格、Issue 模板、安全漏洞披露流程；鼓励社区贡献 NAT 穿透、平台适配与性能优化补丁。

如果你愿意，我可以把上面的计划书与 SRS 草案整理成正式的可下载文档（PDF/Word）、一页项目路线图幻灯片、以及一个 GitHub 仓库的初始 README 与目录结构。我可以为你生成：

完整项目计划书（可直接发布）

详细 Software Specification Document（SRS）（包含需求矩阵、用例、接口定义）

项目时间线甘特图 / 里程碑幻灯片

GitHub 仓库初始结构与 CI 配置示例

如果你要我继续把这些交付物生成成可下载的文档与项目骨架，我可以代为发起任务来执行这些交付物并交付成果。点击开始后我会：整理并导出 （1）项目计划书 PDF、（2）SRS 文档 Word、（3）一页路线图幻灯片、（4）GitHub 仓库初始 README 与目录结构，并附上测试计划与示例 CI 配置。

我现在为你准备启动该任务。

加入新要求：
1 如果需要用到第三方的软件，请使用开源软件（你可以推荐），并一起打包到软件中。
2 防止图片截屏。防止录音、录视频。
好的，Gung。已把你新增的两条要求（只用开源第三方并随软件打包；防止图片截屏、录音、录视频）整合进任务说明，并把交付物范围、格式和时间线都列清楚。下面是我为你整理的要点与下一步计划（直接可交付的内容与验收标准），便于你确认我将要执行的工作。

我将交付的内容（初稿，10 个工作日内）

更新后的项目计划书（PDF）

包含：项目目标、里程碑、资源估算、风险矩阵、开源依赖策略与替代库清单、打包与分发方案、以及针对防截屏/防录音/防录视频的技术方案、平台差异与合规注意事项。

验收要点：明确列出每个第三方库的许可证、打包方式（静态/动态、二进制或源码）、以及对最终发行包体积与合规性的影响。

详细 Software Specification Document（Word）

包含：功能需求、非功能需求、用例、接口定义、数据模型、加密与密钥管理细节、媒体编解码与带宽策略、NAT 穿透方案、日志/隐私策略、测试矩阵、以及防截屏/防录音/防录视频的实现细节（按平台分列）。

验收要点：每项功能对应验收标准与测试用例；防录屏/录音功能列出可实现措施、不可完全防护的风险与替代缓解策略。

GitHub 仓库初始结构与 README（Markdown）

包含：推荐开源依赖（主选与备选）、许可证建议（MIT/Apache 2.0）、构建与打包说明（如何把开源第三方打包进发行版）、安全与隐私声明模板、贡献指南与Issue/PR 模板。

验收要点：README 可直接作为开源项目首页，包含快速上手、构建步骤与许可证合规说明。

CI 配置示例（GitHub Actions，YAML）

自动化构建、单元测试、跨平台打包示例（移动与桌面）、以及开源许可证合规检查步骤（生成依赖清单并校验许可证）。

验收要点：能在示例仓库上跑通一次完整构建流程（示例脚本与占位动作）。

一页路线图幻灯片（PNG）

6 个月里程碑与优先级，突出：局域网直连、加密与阅后即焚、媒体压缩与 320p 限制、开源依赖集成、以及防截屏/防录音/防录视频的开发与测试节点。

验收要点：视觉清晰、可直接用于项目汇报或 README。

关于“只用开源第三方并随软件打包”

我会优先推荐并列出成熟、活跃维护且许可证友好的库（示例：libsodium、libwebrtc、Opus、FFmpeg、libnice、coturn 等），并为每个库提供：许可证类型、打包建议（源码编译或二进制打包）、替代方案与许可证影响评估。

在计划书与 README 中会包含“如何把第三方开源库随发行包打包”的具体步骤与注意事项（例如：静态链接 vs 动态链接、二进制许可合规、二次分发声明）。

关于“防止图片截屏 / 防止录音 / 防止录视频”

我会在 SRS 与计划书中提供：平台能力矩阵（Android / iOS / Windows / macOS / Linux）、可实现的技术措施、无法完全防护的风险说明、用户体验与法律合规注意事项、以及具体测试用例与验证方法。

示例措施（会在文档中详细展开并标注可行性与限制）：

Android：使用 FLAG_SECURE 防止系统截屏/录屏；在音频通话时检测并限制后台录音权限；结合硬件 DRM/安全窗口（若设备支持）降低截屏成功率。

iOS：利用 iOS 的屏幕录制检测回调与 AVAudioSession 权限管理；限制屏幕镜像与录制的可行性说明（受系统限制）。

桌面（Windows/macOS/Linux）：桌面平台无法完全阻止第三方录屏/录音软件；可采用水印、短时密钥、画面模糊/分辨率限制、以及检测常见录屏进程的策略（并说明其局限与隐私/合规风险）。

通用策略：对敏感图片启用可见水印、缩略图仅显示低分辨率预览、在查看敏感媒体时短时降级分辨率并启用可见动态水印（设备 ID / 时间戳），并在 UI 上明确告知用户“无法完全阻止截屏/录制”的风险与法律责任提示。

文档会明确区分“可实现的防护”与“不可完全防护的现实”，并给出法律合规建议（例如在某些司法辖区对录音/截屏的限制与告知义务）。

时间线与交付节奏

初稿交付：10 个工作日内（包含计划书 PDF、SRS Word、README Markdown、CI YAML、路线图 PNG）。

一次修订：收到你的反馈后 3 个工作日内完成修订。
